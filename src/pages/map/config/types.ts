export interface indexState {
  isShowTotal: boolean;
  totalObj: any[];
  formObj: any;
  isShowForm: boolean;
  mapDataInit: any;
  isSavePage: boolean;
  btnType: string;
  moveTypes: string;
}

export interface UrlParams {
  id: string;
  token: string;
}
